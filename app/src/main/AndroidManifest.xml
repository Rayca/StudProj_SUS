<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flaus.susea">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DatenbankTest" />
        <activity android:name=".TestActivity" />
        <activity android:name=".TypActivity"/>
        <activity android:name=".AuswertungsActivities.AuswertungStudieActivity" />
        <activity android:name=".AuswertungsActivities.AuswertungTestActivity" />
        <activity android:name=".ListViewActivities.ListViewTestsActivity" />
        <activity android:name=".ListViewActivities.ListViewStudienActivity" />
        <activity android:name=".AnzeigeErgebnisseTest"
            android:theme="@style/Campusboard"></activity>
    </application>

</manifest>